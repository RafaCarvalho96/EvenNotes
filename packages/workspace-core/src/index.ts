/**
 * List all Markdown files under a directory (stub implementation).
 * Real implementation should use fs/promises readdir with recursive: true.
 */
export async function listFiles(dir: string): Promise<string[]> {
  // stub: returns empty array; replace with real fs.readdir implementation
  void dir;
  return [];
}
